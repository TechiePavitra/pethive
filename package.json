{
  "name": "pethive",
  "version": "1.0.1",
  "description": "PetHive is a full-stack ecommerce petshop and management tool.",
  "author": "Your Name",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/TechiePavitra/pethive"
  },
  "keywords": [
    "framework",
    "full-stack",
    "ecommerce",
    "petshop",
    "saas",
    "ai-friendly",
    "template",
    "starter-kit",
    "react",
    "express",
    "prisma"
  ],
  "scripts": {
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "build": "cd server && npm install && npx prisma generate && cd ../client && npm run build",
    "vercel-build": "cd server && npm install && npx prisma generate && cd ../client && npm run build"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2"
  }
}
